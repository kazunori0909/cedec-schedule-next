# シークレット（.env に記載し、コミットしないこと）
YOUTUBE_API_KEY=your_api_key_here

# Google Analytics 計測 ID（例: G-XXXXXXXXXX）。
# 未設定の場合は Analytics を読み込まない（開発中は空のままにする）。
# 注意: NEXT_PUBLIC_ プレフィックスはブラウザに公開されるため秘密情報ではない値のみ設定する。
NEXT_PUBLIC_GA_MEASUREMENT_ID=
